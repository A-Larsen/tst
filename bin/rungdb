#!/bin/bash

cd "$1"

tstdir=/usr/local/share/tst/
type=(*.*)
clear

for file in ${type[@]}; do
    case $file in
        *.nasm) gdb -x ${tstdir}/asm_next.gdb tst;;
        *.c) gdb tst;;
    esac
done
